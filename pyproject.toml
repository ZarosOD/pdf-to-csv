[project]
name = "pdf-to-csv"
version = "0.1.0"
description = "Extract structured invoice/receipt data from text-layer PDFs into a flat CSV and an .xlsx."
requires-python = ">=3.12"
dependencies = [
    "pdfplumber==0.11.4",
    # Every run with -o writes invoices.xlsx beside invoices.csv, so the
    # workbook writer is not an extra and neither is this.
    "openpyxl==3.1.5",
]

[project.optional-dependencies]
dev = [
    "reportlab==4.2.5",
    "pytest==8.3.3",
]
# Only needed to regenerate demo/out/. Kept out of the default install so
# `make run` does not pull a browser down for a job that does not need one.
demo = [
    "playwright==1.47.0",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["pdf_to_csv"]

[tool.pytest.ini_options]
testpaths = ["tests"]
